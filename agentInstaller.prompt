Tolong Buatkan python  script yang memiliki fungsi untuk generate script berdasarkan os yang dipilih, Linux (All Distr) dan Windows.Script tersebut berisikan langkah langkah untuk Instalasi Agent yang dapat mengirimkan (forwarding) http access.log, error.log, apache, dan semua logging yang tersedia pada linux maupun Windows kepada ingestion server yang akan melakukan filtering terhadap log log yang telah dikirim dari berbagai server/endpoint yang telah terinstall agent tersebut
tolong tambahkan fungsi untuk set konfigurasi Ports, log path, dan semua parameters yang memiliki optional value